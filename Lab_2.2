#include <stdio.h>
#include <locale.h>

void main() {
	
	setlocale(LC_ALL,"Rus" );
	int N, K;

	printf("Сейчас %d часов %d минут 00 секунд ", 12, 45 );
	printf( "\n" "Идет %d минута суток", 46);
	printf( "\n" "До полуночи осталось % d часов и % d минут", 24 - 12-1, 60 - 45 );
	printf( "\n" "С 8:00 прошло %d секунд",12*60*60 + 45*60 - 8*60*60 );
	printf( "\n" "Текущий час %d ", 12);

	return 0;


}
